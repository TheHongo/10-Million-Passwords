#include "Trie.h"
#include "Test.h"
#include "GUI.h"

using namespace std;

int main() {
    // Reading from the file and inserting into the data structure


    Screen screen;
    screen.openWindow(800.0f, 600.0f);
}
